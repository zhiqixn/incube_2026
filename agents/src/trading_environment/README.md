# Set Up

1. requires data files `company_news` and `ticker_price` to enable analyst agent tools
2. configure test case under `runtime_config.py`
3. think and condenser tools not enabled in current implementation
